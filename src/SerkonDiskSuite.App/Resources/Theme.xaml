<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <!--
        Bu dosya artık yalnızca WPF-UI'nin (ui:ThemesDictionary + ui:ControlsDictionary,
        bkz. App.xaml) karşılığı OLMAYAN özel stilleri içerir:
          - DataGrid/DataGridColumnHeader/DataGridRow/DataGridCell: WPF-UI'nin küratörlü
            kontrol setinde DataGrid yok, bu yüzden kendi stilimiz gerekiyor.
          - Heading/Muted/ErrorText: adlandırılmış kısayol TextBlock stilleri (WPF-UI'de
            aynı isimde bir eşdeğeri yok), ama içerik olarak WPF-UI'nin kendi tema
            fırçalarını (TextFillColorPrimaryBrush vb.) kullanıyorlar.
        Buton, TextBox, ComboBox, ScrollBar, ListBox, ProgressBar, TabControl ve "Card"
        stilleri buradan kaldırıldı; bunların hepsi artık WPF-UI'nin kendi implicit
        stilleri (ControlsDictionary) veya doğrudan ui:Button / ui:Card kontrolleri
        üzerinden geliyor.

        Aşağıdaki özel renkler (DataGrid'e özgü) önceki oturumda WCAG AA hedefiyle
        (gövde metni >=4.5:1) hesaplanıp doğrulanmıştı; WPF-UI'nin koyu tema zemini
        (~#202020) ile aynı aile içinde kaldıkları için değerler korunuyor:
          TextFillColorPrimaryBrush/HeaderBackgroundColor  ~15:1
          TextFillColorPrimaryBrush/RowHoverColor          ~10:1
          TextFillColorPrimaryBrush/RowSelectedColor        ~8.8:1
        (WPF-UI'nin kendi TextFillColorPrimaryBrush/TextFillColorSecondaryBrush değerleri
        zaten Microsoft Fluent Dark spesifikasyonuna göre AA/AAA uyumludur; bu üçü hariç
        tüm metin/zemin çiftleri artık WPF-UI'nin kendi doğrulanmış tema fırçalarını
        kullanıyor.)
    -->
    <Color x:Key="HeaderBackgroundColor">#FF0F1114</Color>
    <Color x:Key="RowHoverColor">#FF2E333C</Color>
    <Color x:Key="RowSelectedColor">#FF2C3E5C</Color>
    <Color x:Key="BorderMutedColor">#FF33383F</Color>

    <SolidColorBrush x:Key="HeaderBackgroundBrush" Color="{StaticResource HeaderBackgroundColor}" />
    <SolidColorBrush x:Key="RowHoverBrush" Color="{StaticResource RowHoverColor}" />
    <SolidColorBrush x:Key="RowSelectedBrush" Color="{StaticResource RowSelectedColor}" />
    <SolidColorBrush x:Key="BorderMutedBrush" Color="{StaticResource BorderMutedColor}" />

    <!-- Başlık metni -->
    <Style x:Key="Heading" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="FontSize" Value="18" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Margin" Value="0,0,0,8" />
    </Style>

    <Style x:Key="Muted" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
        <Setter Property="FontSize" Value="12" />
    </Style>

    <!-- Hata/uyarı metni (normal boyut, WPF-UI'nin kritik durum rengi) -->
    <Style x:Key="ErrorText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCriticalBrush}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <!--
        ui:NavigationView, sayfaları kendi iç Frame türevi olan
        NavigationViewContentPresenter üzerinden gösterir. Bu presenter
        varsayılan olarak (IsDynamicScrollViewerEnabled=true) içeriği sonsuz
        yükseklik veren bir ScrollViewer'a sarar; bu yüzden Grid'deki "*"
        satırları doğal (Auto) boyutuna küçülüyor ve tüm sayfa NavigationView
        içinde dikeyde ORTALANIYOR (üstte ve altta büyük boş alan).
        IsDynamicScrollViewerEnabled'ın CLR set erişeni `protected` olduğundan
        (MC3080) burada Setter ile ayarlanamıyor; bu yüzden kapatılması
        MainWindow.xaml.cs'te SetValue ile yapılıyor. Burada sadece Stretch
        hizalaması (public setter'ları var) ayarlanıyor.
    -->
    <Style TargetType="{x:Type ui:NavigationViewContentPresenter}">
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
    </Style>

    <!-- ================= DataGrid ================= -->
    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="RowHeaderWidth" Value="0" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="ColumnHeaderHeight" Value="36" />
        <Setter Property="RowHeight" Value="34" />
        <Setter Property="HeadersVisibility" Value="Column" />
        <Setter Property="GridLinesVisibility" Value="Horizontal" />
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource BorderMutedBrush}" />
        <Setter Property="VerticalGridLinesBrush" Value="{StaticResource BorderMutedBrush}" />
        <Setter Property="AlternationCount" Value="2" />
        <Setter Property="AutoGenerateColumns" Value="False" />
        <Setter Property="IsReadOnly" Value="True" />
        <Setter Property="CanUserAddRows" Value="False" />
        <Setter Property="CanUserReorderColumns" Value="False" />
        <Setter Property="CanUserResizeRows" Value="False" />
        <Setter Property="SelectionMode" Value="Single" />
        <Setter Property="EnableRowVirtualization" Value="True" />
        <Setter Property="HorizontalScrollBarVisibility" Value="Disabled" />
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource HeaderBackgroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderMutedBrush}" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridColumnHeader">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="DataGridRow">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Style.Triggers>
            <Trigger Property="AlternationIndex" Value="1">
                <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource RowHoverBrush}" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource RowSelectedBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="DataGridCell">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="10,6" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border Background="{TemplateBinding Background}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
